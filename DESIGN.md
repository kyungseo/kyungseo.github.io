# Design Principles

이 사이트의 시각·UX 결정 기록이다. 목표는 "글을 편하게 오래 읽을 수 있다"는 인상이며,
기능의 수가 아니라 각 기능이 자기 역할을 정확히 갖는 것을 우선한다.

## 확정 토큰 (2026-08-05)

- 배경 미색(`#fbfaf7`)·청록 accent(`#0f7b6c`), 다크모드는 따뜻한 톤(`#17191a`/`#5fd3bc`).
- 본문 폰트는 시스템 스택(로컬 Pretendard 우선, 없으면 OS 기본). **webfont CDN은 쓰지 않는다** —
  성능·장애 표면과 방문자 제3자 요청 경계를 만들지 않기 위해서다. 실제 KO/EN 장문·모바일에서
  조판 문제가 반복 관측될 때만 pinned self-hosted subset을 별도 검토한다.
- 대비는 WCAG AA 이상을 유지한다 (light accent 4.94:1, dark accent 9.69:1, 본문 14:1대).
  작은 보조 텍스트에는 accent를 쓰지 않는다.

## 구조 원칙

- 본문은 1단 읽기 컬럼(65~75자), mobile-first. 320px에서 가로 스크롤 0.
- 글 상단 메타는 제목·날짜·(번역판 존재 시) 언어 pill만. 시리즈·태그 맥락은 글 하단이 담당한다.
- 언어 전환은 글 단위 개념이다: 번역판이 있는 글에만 메타 영역 pill로 노출하고,
  전역 nav에는 두지 않는다. 번역판이 없는 글은 자기 언어로만 존재한다(자동 이동 없음).
- 본문 링크는 색만으로 구분하지 않는다(은은한 underline + hover/focus 상태).
- 인용문은 논증의 전환점이므로 본문과 같은 명도를 유지한다.
- 접근성은 보이게: skip link, `:focus-visible` outline, reduced-motion 대응.

## 표면별 밀도 원칙

심플함과 단촐함은 다르다 — 단촐함은 개성도 정보 구조도 없는 상태이고, 심플함은 필요한
요소는 선명하고 나머지는 의도적으로 비워 둔 상태다. 이 사이트는 화면마다 밀도를 다르게 가져간다:
**화려함은 홈과 콘텐츠 구조에서, 신뢰는 본문에서.**

| 표면 | 밀도 |
| --- | --- |
| Article 본문 | 절제 유지 — 경쟁력은 "계속 읽고 싶다"이지 "뭘 많이 했네"가 아니다 |
| 홈·시리즈·태그 | featured article, 시리즈 카드, 짧은 소개문, 가벼운 visual rhythm 허용 — 단 글이 쌓인 뒤 도입(빈 진열대 금지) |
| Case study·release성 글 | diagram, evidence card, timeline 등을 **논증에 필요할 때만** |
| Interaction | hover·focus·작은 전환 정도만 정교하게 |

## 지금 넣지 않는 것

읽기 흐름을 우선하기 위해 다음은 현재 도입하지 않는다. 도입하게 되면 위 표의 위치 원칙을 따른다.

- 읽기 진행률 표시, 과한 애니메이션 — 도입 계획 없음
- 고정 sidebar — (도입 시) 긴 guide/reference의 opt-in 목차로만
- 사이트 자체 좋아요·카운터 — 도입하지 않음. 반응 수치를 품질 지표로 승격하지 않는다는
  방침과 정면 충돌. GitHub Discussions 안의 emoji reaction은 허용(우리 표면 밖의 일).

## 소통 경로 (2026-08-05 개정)

제3자 판단 축은 "제3자인가"가 아니라 **필수 기반화 여부·방문자 연결 발생 시점·탈착 가능성**이다.

- **공유**: 본문 하단의 조용한 affordance — `navigator.share`(네이티브) + 링크 복사.
  외부 SDK·추적 위젯은 쓰지 않는다.
- **의견 (1단계, 현재)**: 글 하단 "GitHub에서 의견 남기기" 정적 링크 → repo Discussions.
  사이트는 외부 iframe을 전혀 로드하지 않고, 대화하려는 독자만 GitHub로 이동한다.
- **의견 (2단계, 조건부)**: 본문 내 giscus embed는 "글 안에서 바로 읽고 쓰고 싶다"는
  실수요가 확인될 때만 도입한다. 방식은 **클릭 후 주입**(정적 버튼 → 사용자가 누른 뒤에만
  script/iframe 로드; `loading=lazy`는 명시적 동의가 아니므로 불충분). 승격 전 선행 결정:
  KO/EN 글이 같은 discussion을 공유하도록 공통 식별자(slug 기반 term) 매핑을 확정한다.
- **이미지 확대**: 무의존 `<dialog>` lightbox — 클릭 확대, ESC/클릭 닫기.
